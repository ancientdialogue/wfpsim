name: "Deploy DB"
description: "builds and deploys the gcsim DB to cloudflare. Assumes yarn setup has already ran"
inputs:
  apiToken:
    required: true
    description: cloudflare api token
  accountId:
    required: true
    description: cloudflare account id
  gitHubToken:
    required: true
    description: github token
  branch:
    required: true
    description: name of the branch

runs:
  using: composite
  steps:
    - name: Build UI
      working-directory: ./ui
      shell: bash
      run: yarn build:db

    - name: List UI dist
      working-directory: ./ui/packages/db/dist
      shell: bash
      run: ls -lh

    - name: Publish to Cloudflare Pages
      uses: cloudflare/wrangler-action@v3
      with:
        apiToken: ${{ inputs.apiToken }}
        accountId: ${{ inputs.accountId }}
        workingDirectory: ./ui/packages/db
        command: pages deploy ./dist --project-name="gcsim-db" --branch="${{ inputs.branch }}"
